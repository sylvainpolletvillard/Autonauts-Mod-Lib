--ModLib v0.1

require("Script")
require("Item")
require("Building")
require("Vegetal")
require("Animal")

ModLib = {
    SetScript = SetScript,
    Instruction = Instruction,
    Action = Action,
    Repeat = Repeat,
    If = If,
    Item = Item,
    Building = Building,
    Vegetal = Vegetal,
    Animal = Animal
}
